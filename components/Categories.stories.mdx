import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import Categories from "./Categories";

export const Template = (args) => <Categories {...args} />;

<Meta title="Design-System/Categories" component={Categories} />

# Categories

Used for filtering, has the category special case of "All" to show all items

<Canvas>
  <Story
    name="Default"
    args={{
      setCategory: () => null,
      category: "Category 1",
      categories: { "Category 1": 1, "Category 2": 2 },
    }}
  />
</Canvas>

<ArgsTable of={Categories} story="Default" />

import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import ProjectGrid from "./ProjectGrid";

export const Template = (args) => <ProjectGrid {...args} />;

<Meta title="Design-System/Projects/ProjectGrid" component={ProjectGrid} />

# ProjectGrid

Shows a list of projects. The special category "All" will show all items, otherwise, it will show the items that match with that category.

Projects have the interface:

```
interface Project {
  categories: string[];
  title?: string;
  slug: string;
  description?: string;
}
```

<Canvas>
  <Story
    name="Default"
    args={{
      category: "All",
      portfolios: [
        {
          categories: ["One"],
          title: "Hello",
          description: "World",
          slug: "/hello-world",
        },
        {
          categories: ["Two"],
          title: "Foo",
          description: "Bar",
          slug: "/foo-bar",
        },
      ],
    }}
  />
</Canvas>

<ArgsTable of={ProjectGrid} story="Default" />

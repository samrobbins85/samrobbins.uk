import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";

import PieChart from "./PieChart";

export const Template = (args) => <PieChart {...args} />;

<Meta title="Design-System/Writing/Charts/PieChart" component={PieChart} />

# PieChart

Creates a Pie Chart, in data `x` should be the label and `y` the value

<Canvas>
  <Story
    name="Default"
    decorators={[
      (Story) => (
        <div className="mx-auto prose prose-serif dark:prose-light dark:prose-serifLight">
          <Story />
        </div>
      ),
    ]}
    args={{
      data: [
        { x: "Chrome", y: 64 },
        { x: "Safari", y: 19 },
        { x: "Firefox", y: 4 },
        { x: "Others", y: 19 },
      ],
      height: 300,
      caption: "Browser Market Share",
    }}
  />
</Canvas>

<ArgsTable of={PieChart} story="Default" />
